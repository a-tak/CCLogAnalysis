# 日付ドリルダウン機能の階層別実装プラン

**作成日**: 2026-01-27
**対象**: GroupDetailPage、ProjectDetailPage、SessionDetailPage

---

## 1. 背景と目的

### 現状
- **ProjectsPage（第1階層）**: 日付ドリルダウン機能 ✅ 実装済み
  - 日付クリック → グループ別トークン使用量を表示
  - `/web/src/pages/ProjectsPage.tsx` (272-344行目)

- **GroupDetailPage（第2階層）**: 日付ドリルダウン ❌ なし
- **ProjectDetailPage（第3階層）**: 日付ドリルダウン ❌ なし
- **SessionDetailPage（第4階層）**: 日付フィルタリング ❌ なし

### 要件
1. **GroupDetailPage**: 日付クリック → グループ内のプロジェクト別統計を表示（**ドリルダウン**）
2. **ProjectDetailPage**: 日付クリック → プロジェクト内のセッション別統計を表示（**ドリルダウン**）
3. **SessionDetailPage**: 日付選択 → セッション内のデータをフィルタリング（**フィルタリング**）

### 用語の定義
- **ドリルダウン**: 日付クリック → 下位階層の詳細データを表示（GroupDetailPage、ProjectDetailPage）
- **フィルタリング**: 日付選択 → 同じページ内のデータを絞り込み（SessionDetailPage）

---

## 2. 階層構造と実装方針

```
第1階層: ProjectsPage（ダッシュボード）
  └─ 日付ドリルダウン → グループ別統計 ✅ 実装済み

第2階層: GroupDetailPage
  └─ 日付ドリルダウン → プロジェクト別統計 ← 追加予定（下位階層へ展開）

第3階層: ProjectDetailPage
  └─ 日付ドリルダウン → セッション別統計 ← 追加予定（下位階層へ展開）

第4階層: SessionDetailPage
  └─ 日付フィルタリング → 同ページ内のデータを絞り込み ← 追加予定（フィルタ機能）
      - ツール呼び出しをフィルタ
      - 会話履歴をフィルタ
```

**ポイント**:
- 第1〜3階層: 上位→下位への**ドリルダウン**（新しいデータセットを表示）
- 第4階層: 同じページ内での**フィルタリング**（既存データを絞り込み）

---

## 3. 実装計画

### Phase 1: バックエンドAPI実装

#### 3.1 新規エンドポイント

##### エンドポイント1: グループの日別プロジェクト統計
```
GET /api/groups/{groupId}/daily/{date}
```

**レスポンス型**:
```typescript
interface GroupDailyStatsResponse {
  date: string
  projects: DailyProjectStats[]
}

interface DailyProjectStats {
  projectId: number
  projectName: string
  sessionCount: number
  totalInputTokens: number
  totalOutputTokens: number
  totalCacheCreationTokens: number
  totalCacheReadTokens: number
  totalTokens: number
}
```

**実装ファイル**:
- DB層: `internal/db/group_stats.go` → `GetGroupDailyProjectStats(groupID, date)`
- サービス層: `internal/api/service_db.go` → `GetGroupDailyStats(groupID, date)`
- ハンドラー: `internal/api/handlers_groups.go` → `getGroupDailyStatsHandler`
- ルーティング: `internal/api/router.go`

**SQL設計**:
```sql
SELECT
    p.id as project_id,
    p.name as project_name,
    COUNT(s.id) as session_count,
    COALESCE(SUM(s.total_input_tokens), 0) as total_input_tokens,
    COALESCE(SUM(s.total_output_tokens), 0) as total_output_tokens,
    COALESCE(SUM(s.total_cache_creation_tokens), 0) as total_cache_creation_tokens,
    COALESCE(SUM(s.total_cache_read_tokens), 0) as total_cache_read_tokens
FROM project_group_mappings pgm
INNER JOIN projects p ON pgm.project_id = p.id
INNER JOIN sessions s ON p.id = s.project_id
WHERE pgm.group_id = ? AND DATE(s.start_time) = ?
GROUP BY p.id, p.name
HAVING session_count > 0
ORDER BY (total_input_tokens + total_output_tokens) DESC
```

##### エンドポイント2: プロジェクトの日別セッション統計
```
GET /api/projects/{projectName}/daily/{date}
```

**レスポンス型**:
```typescript
interface ProjectDailyStatsResponse {
  date: string
  sessions: DailySession[]
}

interface DailySession {
  id: string
  gitBranch: string
  startTime: string
  endTime: string
  duration: string
  totalInputTokens: number
  totalOutputTokens: number
  totalCacheCreationTokens: number
  totalCacheReadTokens: number
  totalTokens: number
  errorCount: number
  firstUserMessage: string
}
```

**実装ファイル**:
- DB層: `internal/db/project_stats.go` → `GetProjectDailySessions(projectID, date)`
- サービス層: `internal/api/service_db.go` → `GetProjectDailyStats(projectName, date)`
- ハンドラー: `internal/api/handlers_project_stats.go` → `getProjectDailyStatsHandler`
- ルーティング: `internal/api/router.go`

**SQL設計**:
```sql
SELECT
    id, project_id, git_branch, start_time, end_time,
    total_input_tokens, total_output_tokens,
    total_cache_creation_tokens, total_cache_read_tokens,
    error_count, first_user_message
FROM sessions
WHERE project_id = ? AND DATE(start_time) = ?
ORDER BY start_time DESC
```

#### 3.2 SessionServiceインターフェース拡張

`internal/service/session_service.go`:
```go
type SessionService interface {
    // 既存メソッド...

    // 新規メソッド
    GetGroupDailyStats(groupID int64, date string) (*GroupDailyStatsResponse, error)
    GetProjectDailyStats(projectName string, date string) (*ProjectDailyStatsResponse, error)
}
```

**注意**: MockSessionServiceにも同じメソッドを追加する必要があります。

---

### Phase 2: フロントエンド実装

#### 2.1 型定義とAPIクライアント

**ファイル**: `web/src/lib/api/types.ts`

上記のTypeScript型を追加。

**ファイル**: `web/src/lib/api/client.ts`

```typescript
async getGroupDailyStats(groupId: number, date: string): Promise<GroupDailyStatsResponse> {
  return fetchApi<GroupDailyStatsResponse>(`/groups/${groupId}/daily/${date}`)
}

async getProjectDailyStats(projectName: string, date: string): Promise<ProjectDailyStatsResponse> {
  return fetchApi<ProjectDailyStatsResponse>(`/projects/${encodeURIComponent(projectName)}/daily/${date}`)
}
```

#### 2.2 GroupDetailPage修正

**ファイル**: `web/src/pages/GroupDetailPage.tsx`

**追加内容**:
1. **状態管理**:
   ```typescript
   const [selectedDate, setSelectedDate] = useState<string | null>(null)
   const [dailyStats, setDailyStats] = useState<GroupDailyStatsResponse | null>(null)
   const [dailyLoading, setDailyLoading] = useState(false)
   const [dailyError, setDailyError] = useState<string | null>(null)
   ```

2. **チャートクリックハンドラー**:
   - `period === 'day'` の場合のみ、日付バッジをクリック可能に表示
   - クリック時に `handleDateClick(dateStr)` を呼び出し
   - `api.getGroupDailyStats(groupId, date)` でデータ取得

3. **ドリルダウンパネル**:
   - ProjectsPageの実装（272-344行目）をベースに実装
   - タイトル: 「{日付} のプロジェクト別トークン使用量」
   - プロジェクト一覧テーブル:
     - プロジェクト名（クリック可能リンク → ProjectDetailPageへ遷移）
     - セッション数
     - 入力トークン、出力トークン、合計トークン
   - 閉じるボタン

#### 2.3 ProjectDetailPage修正

**ファイル**: `web/src/pages/ProjectDetailPage.tsx`

**追加内容**:
1. **状態管理**:
   ```typescript
   const [selectedDate, setSelectedDate] = useState<string | null>(null)
   const [dailyStats, setDailyStats] = useState<ProjectDailyStatsResponse | null>(null)
   const [dailyLoading, setDailyLoading] = useState(false)
   const [dailyError, setDailyError] = useState<string | null>(null)
   ```

2. **チャートクリックハンドラー**:
   - `period === 'day'` の場合のみ、日付バッジをクリック可能に表示
   - クリック時に `handleDateClick(dateStr)` を呼び出し
   - `api.getProjectDailyStats(projectName, date)` でデータ取得

3. **ドリルダウンパネル**:
   - ProjectsPageの実装をベースに実装
   - タイトル: 「{日付} のセッション別トークン使用量」
   - セッション一覧テーブル:
     - セッションID（クリック可能リンク → SessionDetailPageへ遷移）
     - ブランチ名
     - 開始時刻
     - 継続時間
     - トークン数（入力/出力/合計）
     - エラー数
     - 最初のユーザーメッセージ（truncate表示）
   - 閉じるボタン

---

### Phase 3: SessionDetailPageの日付フィルタリング

#### 現状
- SessionDetailPageには時系列グラフがない
- ツール呼び出し（Tool Calls）と会話履歴（Conversation History）を表示
- 1セッション = 通常は数時間〜数日間のやり取り

#### 実装方針
SessionDetailPageでは「ドリルダウン」ではなく「**フィルタリング**」を実装します。

**機能**:
- 日付選択UI（カレンダーまたはドロップダウン）を追加
- 日付を選択すると、その日のデータのみを表示
  - ツール呼び出し（Tool Calls）をフィルタ
  - 会話履歴（Conversation History）をフィルタ
- 「すべての日付」を選択すると、フィルタを解除

**UI設計**:
```
┌─────────────────────────────────────┐
│ セッション詳細                        │
├─────────────────────────────────────┤
│ 📅 日付フィルタ: [すべての日付 ▼]    │  ← 新規追加
│   オプション:                         │
│   - すべての日付                      │
│   - 2026-01-27                       │
│   - 2026-01-26                       │
│   - 2026-01-25                       │
├─────────────────────────────────────┤
│ Basic Information                    │
│ Token Summary                        │
│ Model Usage                          │
│ Tool Calls (フィルタ適用)            │
│ Conversation History (フィルタ適用) │
└─────────────────────────────────────┘
```

**実装詳細**:

1. **状態管理**:
   ```typescript
   const [selectedDateFilter, setSelectedDateFilter] = useState<string | null>(null) // null = すべての日付
   ```

2. **日付リスト生成**:
   ```typescript
   // セッション内の全メッセージとツール呼び出しから日付を抽出
   const availableDates = useMemo(() => {
     const dates = new Set<string>()
     session.toolCalls.forEach(call => {
       const date = new Date(call.timestamp).toISOString().split('T')[0]
       dates.add(date)
     })
     session.messages.forEach(msg => {
       const date = new Date(msg.timestamp).toISOString().split('T')[0]
       dates.add(date)
     })
     return Array.from(dates).sort().reverse() // 降順
   }, [session])
   ```

3. **フィルタリングロジック**:
   ```typescript
   const filteredToolCalls = useMemo(() => {
     if (!selectedDateFilter) return session.toolCalls
     return session.toolCalls.filter(call => {
       const callDate = new Date(call.timestamp).toISOString().split('T')[0]
       return callDate === selectedDateFilter
     })
   }, [session.toolCalls, selectedDateFilter])

   const filteredMessages = useMemo(() => {
     if (!selectedDateFilter) return session.messages
     return session.messages.filter(msg => {
       const msgDate = new Date(msg.timestamp).toISOString().split('T')[0]
       return msgDate === selectedDateFilter
     })
   }, [session.messages, selectedDateFilter])
   ```

4. **UI コンポーネント**:
   - shadcn/uiの`Select`コンポーネントを使用
   - セッション情報カードの下に配置
   - フィルタ適用時は、フィルタ中であることを視覚的に表示

**バックエンド変更**:
- **不要** - 既存のSessionDetail APIで十分（フロントエンドのみで完結）

---

## 4. 実装順序

### Step 1: バックエンド（DB層）
1. `internal/db/group_stats.go`: `GetGroupDailyProjectStats` 実装
2. `internal/db/project_stats.go`: `GetProjectDailySessions` 実装
3. テスト作成: `internal/db/group_stats_test.go`, `internal/db/project_stats_test.go`

### Step 2: バックエンド（API層）
4. `internal/api/types.go`: レスポンス型追加
5. `internal/service/session_service.go`: インターフェース拡張
6. `internal/api/service_db.go`: `GetGroupDailyStats`, `GetProjectDailyStats` 実装
7. `internal/api/mock_service.go`: MockSessionServiceへのメソッド追加
8. `internal/api/handlers_groups.go`: `getGroupDailyStatsHandler` 実装
9. `internal/api/handlers_project_stats.go`: `getProjectDailyStatsHandler` 実装
10. `internal/api/router.go`: ルーティング追加
11. テスト作成: ハンドラーテスト

### Step 3: フロントエンド
12. `web/src/lib/api/types.ts`: TypeScript型定義追加
13. `web/src/lib/api/client.ts`: APIメソッド追加
14. `web/src/pages/GroupDetailPage.tsx`: 日付ドリルダウン実装
15. `web/src/pages/ProjectDetailPage.tsx`: 日付ドリルダウン実装
16. テスト作成（必要に応じて）

### Step 4: SessionDetailPage（フィルタリング機能）
17. `web/src/pages/SessionDetailPage.tsx`: 日付フィルタUI追加
18. 日付リスト生成ロジック実装
19. フィルタリングロジック実装（useMemo使用）
20. Select コンポーネントでUI実装

---

## 5. テスト戦略

### 5.1 バックエンドテスト

#### DB層テスト
- **GetGroupDailyProjectStats**:
  - ✅ 正常系: 指定日にセッションが存在
  - ✅ 境界値: セッションがない日付
  - ✅ 異常系: 存在しないグループID
  - ✅ 複数プロジェクトの集計確認
  - ✅ トークン合計の計算正確性

- **GetProjectDailySessions**:
  - ✅ 正常系: 指定日にセッションが存在
  - ✅ 境界値: セッションがない日付
  - ✅ 異常系: 存在しないプロジェクトID
  - ✅ ソート順（開始時刻降順）

#### ハンドラーテスト
- ✅ 正しい日付フォーマット（YYYY-MM-DD）で200 OK
- ✅ 不正な日付フォーマットで400 Bad Request
- ✅ 存在しないグループ/プロジェクトで404 Not Found
- ✅ レスポンスJSON構造の検証

### 5.2 フロントエンドテスト

#### GroupDetailPage / ProjectDetailPage（ドリルダウン機能）
- ✅ APIクライアントのモックテスト
- ✅ チャートクリック時の状態変更
- ✅ ドリルダウンパネルの表示/非表示
- ✅ ローディング状態の表示
- ✅ エラー状態の表示
- ✅ データがない場合の表示

#### SessionDetailPage（フィルタリング機能）
- ✅ 日付リストの生成（useMemoのテスト）
- ✅ フィルタリングロジックのテスト
  - すべての日付表示
  - 特定日付でフィルタ
  - ツール呼び出しのフィルタ
  - 会話履歴のフィルタ
- ✅ 日付選択UIの動作
- ✅ フィルタ状態の視覚的表示

---

## 6. 注意点とリスク

### 6.1 パフォーマンス
- **リスク**: 大量セッションがある日付での処理速度
- **対策**:
  - DB INDEXを活用（start_time, project_id）
  - 必要に応じてページネーション検討

### 6.2 日付フォーマット
- **注意**: タイムゾーンの扱い
  - バックエンド: UTC基準でDATE関数を使用
  - フロントエンド: ローカルタイムゾーンを考慮
- **統一**: ISO 8601形式（YYYY-MM-DD）

### 6.3 UX
- **スクロール**: ドリルダウンカード表示時にスムーズなスクロール
- **視覚的フィードバック**: ローディング状態の明確な表示
- **一貫性**: ProjectsPageと同じUI/UXパターンを維持

### 6.4 既存機能への影響
- **SessionServiceインターフェース変更**: 全実装クラスを同時更新
- **MockSessionServiceへのメソッド追加**: テストコードも更新

---

## 7. 重要ファイル一覧

### バックエンド
- `internal/db/group_stats.go` - DB層: グループの日別プロジェクト統計
- `internal/db/project_stats.go` - DB層: プロジェクトの日別セッション取得
- `internal/api/types.go` - API型定義
- `internal/api/service_db.go` - サービス層実装
- `internal/service/session_service.go` - インターフェース定義
- `internal/api/mock_service.go` - モックサービス
- `internal/api/handlers_groups.go` - グループハンドラー
- `internal/api/handlers_project_stats.go` - プロジェクトハンドラー
- `internal/api/router.go` - ルーティング

### フロントエンド
- `web/src/lib/api/types.ts` - TypeScript型定義
- `web/src/lib/api/client.ts` - APIクライアント
- `web/src/pages/GroupDetailPage.tsx` - グループ詳細ページ（日付ドリルダウン追加）
- `web/src/pages/ProjectDetailPage.tsx` - プロジェクト詳細ページ（日付ドリルダウン追加）
- `web/src/pages/SessionDetailPage.tsx` - セッション詳細ページ（日付フィルタリング追加）

### 参考実装
- `web/src/pages/ProjectsPage.tsx` (272-344行目) - 既存の日付ドリルダウン実装
- `web/src/components/ui/select.tsx` - shadcn/ui Select コンポーネント（日付フィルタで使用）

---

## 8. 完成基準

- [ ] バックエンドAPI（2エンドポイント）が正常動作
- [ ] 全テストがパス（バックエンド、フロントエンド）
- [ ] GroupDetailPageでチャートクリック→プロジェクト一覧表示
- [ ] ProjectDetailPageでチャートクリック→セッション一覧表示
- [ ] SessionDetailPageの日付フィルタリング実装
- [ ] エラーハンドリングが適切に機能
- [ ] ローディング状態が正しく表示
- [ ] コードレビュー完了
- [ ] ドキュメント更新（API設計.md、README.md）

---

## 9. 検証方法

### バックエンド検証
1. 単体テストを実行: `go test ./internal/db/... -v`
2. APIハンドラーテストを実行: `go test ./internal/api/... -v`
3. サーバー起動: `/server-management start dev`
4. curlでAPIエンドポイント確認:
   ```bash
   curl http://localhost:8080/api/groups/1/daily/2026-01-27
   curl http://localhost:8080/api/projects/MyProject/daily/2026-01-27
   ```

### フロントエンド検証

#### GroupDetailPage / ProjectDetailPageの検証
1. 開発サーバー起動: `cd web && npm run dev`
2. GroupDetailPageにアクセス
3. 日別表示に切り替え
4. グラフの日付バッジをクリック
5. プロジェクト一覧が表示されることを確認
6. プロジェクト名をクリックして詳細ページへ遷移確認
7. ProjectDetailPageでも同様に確認
   - 日付クリック → セッション一覧表示
   - セッションIDクリック → セッション詳細ページへ遷移

#### SessionDetailPageの検証
1. SessionDetailPageにアクセス
2. 日付フィルタUIが表示されることを確認
3. 「すべての日付」選択時: 全データが表示される
4. 特定日付を選択: その日のデータのみ表示される
   - ツール呼び出しがフィルタされる
   - 会話履歴がフィルタされる
5. フィルタ適用中であることが視覚的に分かる
6. 日付を切り替えて動作確認

---

## 10. 次のステップ

### 実装の流れ
1. **TDDに従ってテストを先に作成**
2. **バックエンド実装**（DB層 → サービス層 → ハンドラー）
   - GroupDetailPage用API
   - ProjectDetailPage用API
3. **フロントエンド実装**（API型定義 → 各ページ修正）
   - GroupDetailPage: 日付ドリルダウン
   - ProjectDetailPage: 日付ドリルダウン
   - SessionDetailPage: 日付フィルタリング
4. **統合テスト**
5. **ドキュメント更新**
   - `docs/API設計.md`: 新規エンドポイント追加
   - `README.md`: 機能説明更新

### 実装優先順位
推奨順序:
1. GroupDetailPage（バックエンド + フロントエンド）
2. ProjectDetailPage（バックエンド + フロントエンド）
3. SessionDetailPage（フロントエンドのみ）

理由: 各階層を完成させてから次に進むことで、動作確認がしやすい
