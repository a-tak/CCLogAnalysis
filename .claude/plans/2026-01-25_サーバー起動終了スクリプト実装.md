# サーバー起動・終了スクリプト実装計画

## 概要

Claude Codeからサーバーの起動・終了を簡単に行うためのスクリプトを作成し、許可コマンドに追加します。

## 目的

- サーバーの起動・停止をスクリプトで自動化
- プロセスID管理により、確実な停止を実現
- Claude Codeからの実行を許可コマンドで許可

## 実装内容

### 1. ディレクトリ構成

```
.claude/
└── skills/
    └── server-management/
        ├── scripts/
        │   ├── start-server.sh    # サーバー起動スクリプト
        │   └── stop-server.sh     # サーバー終了スクリプト
        └── .server.pid            # PIDファイル（自動生成、.gitignore対象）
```

**配置理由:**
- 既存のスキル構成（cr-worktree, ready-for-pr, self-review）と統一
- `.claude/`配下でClaude Code関連スクリプトを一元管理
- 将来的にスキル化（`/start-server`, `/stop-server`）が容易

### 2. start-server.sh の実装

#### 機能

1. 既存プロセスチェック（起動済みならエラー）
2. モード選択（dev/prod）
3. バックグラウンドでサーバー起動
4. PIDを `.claude/skills/server-management/.server.pid` に記録
5. ヘルスチェック（http://localhost:8080/api/health）
6. 起動成功/失敗の通知

#### 引数

- `[dev|prod]` - 起動モード（デフォルト: dev）
  - **dev**: CORS有効、ファイル監視有効（開発用）
  - **prod**: ビルド後に実行（本番用）

#### 環境変数（開発モード）

- `PORT=8080`
- `ENABLE_CORS=true`
- `ENABLE_FILE_WATCH=true`
- `FILE_WATCH_INTERVAL=15`
- `FILE_WATCH_DEBOUNCE=5`

#### 主要ロジック

```bash
#!/bin/bash
set -e

PID_FILE=".claude/skills/server-management/.server.pid"
HEALTH_URL="http://localhost:8080/api/health"
MODE="${1:-dev}"

# 1. 既存プロセスチェック
if [ -f "$PID_FILE" ]; then
  PID=$(cat "$PID_FILE")
  if kill -0 "$PID" 2>/dev/null; then
    echo "❌ エラー: サーバーは既に起動中です（PID: $PID）"
    exit 1
  else
    rm "$PID_FILE"
  fi
fi

# 2. モード別起動
if [ "$MODE" = "dev" ]; then
  echo "🔧 開発モードでサーバーを起動します..."
  export PORT=8080
  export ENABLE_CORS=true
  export ENABLE_FILE_WATCH=true
  export FILE_WATCH_INTERVAL=15
  export FILE_WATCH_DEBOUNCE=5

  nohup make dev > .claude/skills/server-management/server.log 2>&1 &
  PID=$!

elif [ "$MODE" = "prod" ]; then
  echo "🚀 本番モードでサーバーを起動します..."
  make build
  nohup ./bin/ccloganalysis > .claude/skills/server-management/server.log 2>&1 &
  PID=$!
else
  echo "❌ エラー: 不明なモード '$MODE'"
  exit 1
fi

# 3. PID記録
echo "$PID" > "$PID_FILE"
echo "📝 PIDファイルに記録しました: $PID"

# 4. ヘルスチェック（最大30秒待機）
echo "🔍 サーバーの起動を確認中..."
RETRY_COUNT=0
MAX_RETRIES=30

while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
  if curl -s -f "$HEALTH_URL" > /dev/null 2>&1; then
    echo "✅ サーバーが正常に起動しました"
    echo "   URL: http://localhost:8080"
    echo "   PID: $PID"
    echo "   ログ: .claude/skills/server-management/server.log"
    exit 0
  fi

  sleep 1
  RETRY_COUNT=$((RETRY_COUNT + 1))
done

echo "❌ エラー: サーバーの起動に失敗しました"
echo "   ログを確認してください: .claude/skills/server-management/server.log"
exit 1
```

### 3. stop-server.sh の実装

#### 機能

1. PIDファイル存在確認
2. プロセス存在確認
3. Graceful Shutdown（SIGTERM送信）
4. 最大10秒待機
5. 必要に応じてSIGKILL送信
6. PIDファイル削除
7. 終了確認通知

#### 引数

なし

#### 主要ロジック

```bash
#!/bin/bash
set -e

PID_FILE=".claude/skills/server-management/.server.pid"
SIGTERM_TIMEOUT=10

# 1. PIDファイルチェック
if [ ! -f "$PID_FILE" ]; then
  echo "⚠️  警告: PIDファイルが見つかりません"
  echo "   サーバーはスクリプト以外の方法で起動された可能性があります"
  echo "   手動でプロセスを確認してください: ps aux | grep ccloganalysis"
  exit 1
fi

# 2. PID読み取り
PID=$(cat "$PID_FILE")
echo "📝 PIDファイルから読み取り: $PID"

# 3. プロセス存在確認
if ! kill -0 "$PID" 2>/dev/null; then
  echo "⚠️  警告: プロセス $PID は既に終了しています"
  rm "$PID_FILE"
  echo "✅ PIDファイルをクリーンアップしました"
  exit 0
fi

# 4. Graceful Shutdown（SIGTERM）
echo "🛑 サーバーを停止中... (PID: $PID)"
kill -TERM "$PID"

# 5. 最大10秒待機
WAIT_COUNT=0
while [ $WAIT_COUNT -lt $SIGTERM_TIMEOUT ]; do
  if ! kill -0 "$PID" 2>/dev/null; then
    echo "✅ サーバーが正常に停止しました"
    rm "$PID_FILE"
    exit 0
  fi

  sleep 1
  WAIT_COUNT=$((WAIT_COUNT + 1))
  echo -n "."
done

echo ""
echo "⚠️  警告: Graceful shutdownがタイムアウトしました"
echo "   強制終了します..."

# 6. 強制終了（SIGKILL）
kill -KILL "$PID" 2>/dev/null || true
sleep 1

if ! kill -0 "$PID" 2>/dev/null; then
  echo "✅ サーバーを強制終了しました"
  rm "$PID_FILE"
  exit 0
else
  echo "❌ エラー: サーバーの停止に失敗しました"
  exit 1
fi
```

### 4. 許可コマンド設定

`.claude/settings.json` の `permissions.allow` 配列に以下を追加:

```json
{
  "permissions": {
    "allow": [
      "Bash(.claude/skills/server-management/scripts/start-server.sh:*)",
      "Bash(.claude/skills/server-management/scripts/stop-server.sh:*)",
      "Bash(go test:*)",
      "Bash(make build-frontend:*)",
      "Bash(npm run lint:*)"
    ]
  }
}
```

**注意点:**
- アルファベット順にソート
- 既存の許可コマンドと統一されたフォーマット

### 5. .gitignore の更新

`.claude/skills/server-management/.server.pid` を除外対象に追加:

```gitignore
# サーバー管理スクリプト
.claude/skills/server-management/.server.pid
```

**注意:** `*.log` は既に除外されているため、`server.log` は自動的に除外される。

### 6. README.md の更新

「開発」セクション（137行目以降）に新しいサブセクションを追加:

```markdown
### サーバー管理スクリプト

Claude Codeからサーバーの起動・停止を簡単に行うためのスクリプトを提供しています。

**起動:**
```bash
# 開発モード（CORS有効、ファイル監視有効）
.claude/skills/server-management/scripts/start-server.sh dev

# 本番モード（ビルド後に起動）
.claude/skills/server-management/scripts/start-server.sh prod
```

**停止:**
```bash
.claude/skills/server-management/scripts/stop-server.sh
```

**ログ確認:**
```bash
tail -f .claude/skills/server-management/server.log
```

**開発モードのデフォルト環境変数:**
- `PORT=8080`
- `ENABLE_CORS=true`
- `ENABLE_FILE_WATCH=true`
- `FILE_WATCH_INTERVAL=15`
- `FILE_WATCH_DEBOUNCE=5`
```

**挿入位置:** 「### 開発モード」セクション（138～156行目）の後に追加

## 実装手順

### ステップ1: ディレクトリ作成

```bash
mkdir -p .claude/skills/server-management/scripts
```

### ステップ2: start-server.sh 作成

- ファイルパス: `.claude/skills/server-management/scripts/start-server.sh`
- 実行権限を付与: `chmod +x start-server.sh`

### ステップ3: stop-server.sh 作成

- ファイルパス: `.claude/skills/server-management/scripts/stop-server.sh`
- 実行権限を付与: `chmod +x stop-server.sh`

### ステップ4: .gitignore 更新

- PIDファイルの除外設定を追加

### ステップ5: 許可コマンド追加

- `.claude/settings.json` にスクリプトの許可を追加
- アルファベット順にソート

### ステップ6: README.md 更新

- 「開発」セクションに「サーバー管理スクリプト」サブセクションを追加

### ステップ7: テスト

```bash
# 起動テスト
.claude/skills/server-management/scripts/start-server.sh dev

# 動作確認
curl http://localhost:8080/api/health

# 終了テスト
.claude/skills/server-management/scripts/stop-server.sh

# 再起動テスト
.claude/skills/server-management/scripts/start-server.sh dev
.claude/skills/server-management/scripts/stop-server.sh

# 本番モードテスト
.claude/skills/server-management/scripts/start-server.sh prod
curl http://localhost:8080/api/health
.claude/skills/server-management/scripts/stop-server.sh
```

## Makefileとの関係

**共存方針:**
- Makefileコマンド（`make dev`, `make run`）は残す → 人間の開発者が使用
- スクリプト（`start-server.sh`, `stop-server.sh`）→ Claude Code が使用
- スクリプトは内部で `make dev` や `make build && ./bin/ccloganalysis` を呼び出す

**利点:**
- ビルドロジックの重複を避ける
- Makefileを Single Source of Truth として維持
- スクリプトはプロセス管理とPID管理に専念

## エラーハンドリング

### ケース1: PIDファイルは存在するがプロセスが死んでいる

- **start-server.sh**: PIDファイルを削除して起動継続
- **stop-server.sh**: PIDファイルをクリーンアップして正常終了

### ケース2: スクリプト以外の方法で起動されたサーバー

- **start-server.sh**: 既存プロセスをチェックして警告
- **stop-server.sh**: PIDファイル不在を警告して手動停止を案内

### ケース3: ビルド失敗（prodモード）

- **start-server.sh**: `make build` の終了コードをチェック → エラー表示（`set -e`により自動終了）

## 検証方法

### 手動テスト

1. **正常起動・停止**
   - 開発モードでサーバーを起動
   - ヘルスチェックで動作確認
   - スクリプトでサーバーを停止

2. **重複起動防止**
   - サーバー起動中に再度起動を試みる
   - エラーメッセージを確認

3. **本番モード**
   - 本番モードでビルド→起動
   - ヘルスチェックで動作確認
   - スクリプトでサーバーを停止

4. **Graceful Shutdown**
   - サーバー起動後、ログでSIGTERM受信を確認
   - 正常に停止することを確認

5. **強制終了**
   - プロセスを一時停止（`kill -STOP`）
   - スクリプトでSIGKILL実行を確認

### 期待される動作

- ✅ 開発モードで正常に起動・停止できる
- ✅ 本番モードで正常に起動・停止できる
- ✅ 重複起動を防止できる
- ✅ Graceful Shutdownが機能する
- ✅ タイムアウト時に強制終了される
- ✅ PIDファイルが正しく管理される
- ✅ エラーメッセージが分かりやすい

## Critical Files

実装に必要な主要ファイル:

- `.claude/skills/server-management/scripts/start-server.sh` - 新規作成
- `.claude/skills/server-management/scripts/stop-server.sh` - 新規作成
- `.claude/settings.json` - 許可コマンド追加（11-14行目）
- `.gitignore` - PIDファイル除外設定追加
- `README.md` - サーバー管理スクリプトセクション追加（138行目以降）

## 将来的な拡張

### スキル化

`.claude/skills/server-management/SKILL.md` を作成し、`/start-server`, `/stop-server` コマンドとして実行可能にする。

### ステータス確認スクリプト

`status-server.sh` を作成し、以下を表示:
- プロセス状態
- 起動時間
- メモリ使用量
- ヘルスエンドポイント応答

### 再起動スクリプト

`restart-server.sh` を作成し、`stop-server.sh` → `start-server.sh` を連続実行。

### ログローテーション

`server.log` が大きくなりすぎないよう、日付別にローテーション。
